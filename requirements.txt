# =============================================================================
# AI-IP-Recommender (Smart IP Advisor) — Python FastAPI backend
# =============================================================================
# Install:
#     pip install -r requirements.txt
#
# Run:
#     uvicorn main:app --reload --port 8000
# =============================================================================

# Web framework + ASGI server
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Request body / form / file handling
python-multipart==0.0.20

# Environment variable loading (.env)
python-dotenv==1.0.1

# Data validation (already pulled in by FastAPI, pinned for clarity)
pydantic==2.10.4

# Azure OpenAI client
openai==1.59.6

# Excel parsing (.xlsx / .xls)
openpyxl==3.1.5
xlrd==2.0.1
pandas==2.2.3
